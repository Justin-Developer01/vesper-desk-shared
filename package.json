{
  "name": "@vesper-desk/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared design tokens, TypeScript types, and API contract stubs for Vesper Desk (Electron) and vesper-desk-web (marketing site).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
